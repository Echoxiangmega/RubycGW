# Research notes archive

These documents record specialized derivations, diagnostics, validation studies,
and historical workflows from the development of RubycGW.  They are preserved
for reproducibility, but command paths and internal APIs may reflect the layout
at the time each note was written.

For current installation, public API, supported workflows, and repository
layout, use the documentation one level above in `docs/`.
